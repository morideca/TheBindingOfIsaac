using System.Collections.Generic;
using UnityEngine;

public class LevelLoader 
{
    private Map _map;

    private Dictionary<Vector2Int, RoomData> mapData;
    

    public void LoadLevel(int stage)
    {
        // mapData = _map.GenerateMap(10);
        
    }
}
